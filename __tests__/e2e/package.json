{
  "name": "vip-go-mu-plugins-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "E2E Tests for mu-plugins",
  "scripts": {
    "setup-env": "./bin/setup-env.sh",
    "destroy-env": "rm -f ../../e2e-parsely-consent.php && vip dev-env destroy --slug=e2e-test-site",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run setup-env",
    "test": "playwright test -c playwright.config.ts",
    "posttest": "npm run destroy-env"
  },
  "author": "Automattic",
  "devDependencies": {
    "@automattic/eslint-plugin-wpvip": "^1.2.1",
    "@playwright/test": "^1.59.1",
    "@types/node": "^26.0.1",
    "asana-phrase": "^0.0.8",
    "eslint-plugin-playwright": "^2.10.2",
    "typescript": "^6.0.3"
  }
}
