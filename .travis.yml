language: php

php:
  - 5.6

before_install:
  - sudo apt-get update > /dev/null
  - ./ci/prepare.sh

script:
  # PHP Lint
  - >
    echo "Running PHP lint";
    find "${TRAVIS_BUILD_DIR}" -name \*.php | parallel --gnu php -d display_errors=stderr -l {} > /dev/null \;
